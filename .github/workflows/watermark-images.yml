name: Watermark project images

# Disabled: deploy-site.yml runs `npm run watermark:projects` before build.
# Re-enable only if you remove the prebuild/deploy watermark step.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  watermark:
    runs-on: ubuntu-latest
    if: false

    steps:
      - run: echo "Use deploy-site workflow instead."
